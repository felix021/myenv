#!/bin/bash

#compile php extension

#modify this when necessary
PHP_ROOT=/opt/php/

$PHP_ROOT/bin/phpize

./configure --with-php-config=$PHP_ROOT/bin/php-config

if make -j 2; then
    ls -l .libs/*.so
fi
