#!/bin/sh
ssh -x

SSH_USER=felix021
SSH_HOST=test.com
SSH_PORT=22

LISTEN_HOST=127.0.0.1
LISTEN_PORT=2022

LOCAL_HOST=127.0.0.1
LOCAL_PORT=22

screen autossh -M 20000 $SSH_USER@$SSH_HOST -p $SSH_PORT -R $LISTEN_HOST:$LISTEN_PORT:$LOCAL_HOST:$LOCAL_PORT
